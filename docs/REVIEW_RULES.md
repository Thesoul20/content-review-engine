# Review Rules

This document has been superseded by [Rule System](./RULES.md).

Use `docs/RULES.md` as the canonical reference for:

- stable `rule_id` values
- built-in rule behavior
- severity levels and ordering
- suppression comments
- quality-gate behavior
- rule counts and severity counts
- report behavior
- current limitations
